{
    "name": "Rank Math MCP Ability",
    "slug": "rank-math-mcp-ability",
    "version": "2.1.0",
    "author": "bucagdas",
    "homepage": "https://github.com/bucagdas/wp-mcp-bridges/tree/main/rank-math-mcp-ability",
    "requires": "7.0",
    "tested": "7.0",
    "requires_php": "8.0",
    "download_url": "https://github.com/bucagdas/wp-mcp-bridges/releases/download/rank-math-mcp-ability-v2.1.0/rank-math-mcp-ability.zip",
    "last_updated": "2026-08-31 12:50:09",
    "sections": {
        "description": "Full-coverage Rank Math SEO abilities for MCP. Per-post SEO metadata (core, robots, social, schema), settings, redirections, 404 monitor, sitemap tools, module toggling and analytics status.",
        "changelog": "<h4>2.1.0</h4><ul><li>Nine ordinary settings were being hidden as if they were secrets: the sensitive-key pattern matched the &quot;auth&quot; inside &quot;author&quot;, so <code>author_robots</code>, <code>url_author_base</code>, <code>disable_author_archives</code> and their siblings could be neither read nor written. Checked against all 182 keys in the four settings groups; only <code>indexnow_api_key</code> is redacted now.</li><li><code>update-settings</code> now refreshes Rank Math&#39;s own settings object after writing. It previously left everything reading <code>Helper::get_settings()</code> in the same request looking at the old value, with no error to show for it.</li><li>Descriptions of the four abilities that overlap Rank Math 1.0.277&#39;s new native settings abilities now name the counterpart and the difference. Nothing was removed.</li><li>Declared a type for the untyped <code>value</code> properties, which stops core&#39;s schema validator warning on every call.</li></ul><h4>2.0.4</h4><ul><li>Fixes silently stale sitemaps after programmatic writes. Rank Math only registers its sitemap cache watcher when is_admin() or wp_doing_cron() is true, so nothing invalidates the cached sitemap XML for writes made over the REST API (which is how this bridge \u2014 and the block editor itself \u2014 saves) or WP-CLI. This bridge now registers the same hooks in those contexts and routes them to Rank Math's own public Cache_Watcher methods rather than implementing its own cache clearing.</li><li>Covers posts, terms and authors, not just posts. Multiple writes in one request coalesce into a single cache invalidation via Rank Math's own queue and shutdown drain.</li><li>Verified end to end over HTTP: with a warm cache, creating 3 posts left post-sitemap.xml serving 4 URLs; after this fix the same URL returns 7.</li></ul><h4>2.0.3</h4><ul><li>Fixed the MCP Adapter's execute-ability tool masking schema errors as a generic \"Permission denied\": get-post-seo, update-post-seo, update-post-social, update-post-schema and delete-post-schema now accept id (preferred) alongside post_id (deprecated alias, kept for backward compatibility), and return a clear WP_Error when neither is provided instead of silently defaulting to 0.</li></ul><h4>2.0.2</h4><ul><li>Security audit: strip_sensitive() no longer removes a matched key outright. A key like connect_data matches on its own name (contains connect) even though most of what it holds isn't secret; silently dropping the whole key made sibling data disappear with no indication it ever existed. The matched key's value is now replaced with a REDACTED marker instead \u2014 not a leak fix (nothing new is exposed), an output-honesty fix.</li></ul><h4>2.0.1</h4><ul><li>CRITICAL: fixed a data-corruption bug \u2014 update-post-seo, update-post-social, update-post-schema and bulk-update-seo-meta were not wp_slash()-ing content before writing to update_post_meta(), causing WordPress&#39;s own unslash step to strip literal backslashes from SEO metadata. Fixed at every affected call site. Settings and redirections/404 monitor were verified safe and left untouched.</li></ul><h4>2.0.0</h4><ul><li>Initial public release: 24 abilities covering post SEO metadata, settings, redirections, 404 monitor, sitemap and module toggling.</li></ul>"
    }
}
