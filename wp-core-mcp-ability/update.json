{
    "name": "WP Core MCP Ability",
    "slug": "wp-core-mcp-ability",
    "version": "1.3.4",
    "author": "bucagdas",
    "homepage": "https://github.com/bucagdas/wp-mcp-bridges/tree/main/wp-core-mcp-ability",
    "requires": "7.0",
    "tested": "7.0",
    "requires_php": "8.0",
    "download_url": "https://github.com/bucagdas/wp-mcp-bridges/releases/download/wp-core-mcp-ability-v1.3.4/wp-core-mcp-ability.zip",
    "last_updated": "2026-08-08 14:24:43",
    "sections": {
        "description": "Full-coverage WordPress core abilities for MCP. Exposes WordPress's own native core/* abilities to MCP, plus generic options, posts, taxonomies/terms, comments and users CRUD.",
        "changelog": "<h4>1.3.4</h4><ul><li>Fixed the MCP Adapter's execute-ability tool masking schema errors as a generic \"Permission denied\": 8 permission callbacks (comments, posts, revisions, users) now return a clear WP_Error when the expected id parameter is missing or invalid, instead of silently defaulting to 0.</li><li>Fixed a real bug found during regression testing: permission_restore_revision's stale bool return type caused a TypeError once its delegate could return WP_Error.</li></ul><h4>1.3.3</h4><ul><li>Security audit: closed a status-field privilege escalation in create-post/update-post (a Contributor could bypass the publish_posts capability via status:publish/private/future). status is now a fixed enum (draft|pending|publish|private|future) and never accepts trash \u2014 use delete-post, which has its own confirm+capability gate.</li><li>future status now requires a genuinely future date, verified server-side (WordPress silently downgrades a non-future future status to an immediate publish, which would otherwise be a second bypass).</li><li>update-option's writable whitelist narrowed: siteurl and home removed entirely (not just confirm-gated), since a wrong value can make the whole site unreachable through the web layer with no recovery path via this API. Still readable via get-option/list-options. admin_email now requires confirm: true.</li></ul><h4>1.3.2</h4><ul><li>CRITICAL: fixed a data-corruption bug \u2014 writes to posts, post meta, terms, comments, users, user meta, nav menus and media uploads were not wp_slash()-ed before being passed to WordPress core functions that expect slashed data, causing WordPress&#39;s own unslash step to strip literal backslashes from content (e.g. JSON-escaped characters inside Gutenberg block attributes). Fixed at 9 call sites across create/update/patch-post, update-post-meta, upload-media, create/update-term, create/update-user, create-comment, create-menu and add/update-menu-item.</li><li>patch-post&#39;s integrity_verified field now re-reads the actual database value after writing and compares it byte-for-byte to what was intended, instead of trusting a pre-write computation.</li></ul><h4>1.3.1</h4><ul><li>Initial public release: 59 abilities covering options, posts (including revisions and a surgical patch-post find/replace verb), taxonomies/terms, comments, users, menus, block widgets, media, plugin/theme status, Site Health, cron and rewrite rules, plus native core/* ability exposure.</li></ul>"
    }
}
